[tasks.dev]
description = "Run the app in development mode"
run = "uv run mandarin-app"

[tasks.fix]
description = "Lint and format code"
run = "uv run ruff check --fix && uv run ruff format"

[tasks.sync]
description = "Sync dependencies"
run = "uv sync"
